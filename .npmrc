; recharts (and other libs) declare a stable react peer range that npm's
; semver will not match against React 19's prerelease tag
; (19.0.0-rc-...). Next 15.0.x ships that RC, so peer resolution fails
; without this. Remove once the project moves to a stable React 19.
legacy-peer-deps=true
